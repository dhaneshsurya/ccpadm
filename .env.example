SECRET_KEY=change-me-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,13.49.229.197,csaconline.in,www.csaconline.in

# SQL Server source (for import_from_sqlserver command)
MSSQL_SERVER=.\SQLEXPRESS
MSSQL_CCPDB=ccpdb
MSSQL_COURSEDB=courseinformation
MSSQL_TRUSTED_CONNECTION=yes

# Email (required for registration / forgot-password OTP)
# Gmail: enable 2-Step Verification, then create an App Password at
# https://myaccount.google.com/apppasswords — use that 16-char password below (not your login password).
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=onlinecsac@gmail.com
EMAIL_HOST_PASSWORD=your-gmail-app-password-here
DEFAULT_FROM_EMAIL=onlinecsac@gmail.com